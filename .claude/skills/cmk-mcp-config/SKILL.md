---
name: cmk:mcp-config
description: This skill should be used when the user asks to "set up MCP", "configure MCP servers", "add serena", "connect the tracker MCP", ".mcp.json", or needs a checked-in, per-vendor Model Context Protocol server configuration for a repo.
version: 0.2.0
---

# cmk-mcp-config

This file is Claude Code's native discovery entry point. Before acting, read
`.agents/skills/cmk-mcp-config/SKILL.md` completely and apply it.

The Claude Code adapter supplies discovery only; the shared skill carries the
guidance.
